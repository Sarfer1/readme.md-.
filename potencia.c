#include <stdio.h>

int main() {
    int potencia = 2 * 2 * 2;
    int m = potencia;
    for(int i = 2; i < 8; i = m) {
    printf("El numero i es %d\n", m);
    }
        
        return 0;
    }
    