#include <stdio.h>
int main()  {
    int o = 2;
    int d = 3;
    int g = 1;
    int q = g * g / o * g / d;
    for(int i = 0; i < 1; i = q) {
        printf("El fraccionario es %d\n", q);
    }
       
    
    }
        